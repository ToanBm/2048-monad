# Essential environment variables for Vercel deployment

# Get from Privy Dashboard: https://console.privy.io/
NEXT_PUBLIC_PRIVY_APP_ID=your_privy_app_id_here

# Monad Games ID App ID (don't change this)
NEXT_PUBLIC_MONAD_APP_ID=cmd8euall0037le0my79qpz42

# Monad Games ID Portal URL (don't change this)
NEXT_PUBLIC_MONAD_PORTAL_URL=https://monad-games-id-site.vercel.app

# Your VPS backend URL
NEXT_PUBLIC_API_BASE_URL=https://your-vps-ip:3001

# Note: Backend provides these endpoints:
# - /api/update-player-data (calls smart contract)
# - /api/add-player-to-list (only adds to backend list)
# - /api/get-player-data (reads from smart contract)
# - /api/get-leaderboard (reads from smart contract)

# Game contract address (optional, has default value)
# NEXT_PUBLIC_GAME_ADDRESS=0x732C3356FE0718f4dA2e821838016Dc5083cab10

# Optional: Set to "true" to disable backend (for testing)
# NEXT_PUBLIC_DISABLE_BACKEND=false
